1) Build email client: this client will have multiple jobs such as emailing user's password reset tokens/links. It will also need to send out emails to any provided recipiant for anonymous survey links (safe and isolated anonymous survey funtionality not yet developed/planned out). Email client and credentials not yet provided to utilize placeholders for now.
2) Develop anonymous survey funtionality. Plan a safe bifercation between logged in users and anonymous survey takers who do not have, nor should they need an account to take.
3) Build password rest logic: users should be able to request a password reset from either a logged in state via the user's profile/settings as well as from the login screen (Forgot password?).
4) Get more user info via profile to collect additional user demogrphics. User profile should contain user metadata as well as some user survey statistics.
5) Increase the amount of base user related functionality the admin has (view user profile info, initiate a password rest...).
6) For choice-based questions, the admin can add an “Other” option that lets the survey taker type their own answer. Add support for an optional “Other” answer choice on applicable question types. For multiple-choice, checkbox, dropdown, and similar option-based questions, admins should be able to enable an “Allow Other” setting. When enabled, the survey-taking UI should automatically render an “Other” option with a free-text input field for the respondent’s custom answer.
The “Other” option should be system-generated and not treated as a normal manually entered answer option. Store the selected Other response separately from standard option IDs, including the custom text entered by the respondent.
7) Superseded: the original per-question definitions tooltip idea in prompt_22 has been replaced by a larger global glossary direction. Do not implement prompt_22 as originally scoped. See prompts/prompt_28.txt, prompts/prompt_29.txt, and prompts/prompt_30.txt for the staged glossary plan.
8) Superseded: the original frontend-only estimated time remaining idea in prompt_23 has been replaced by a larger backend-owned timing direction. Do not implement prompt_23 as originally scoped. See prompts/prompt_25.txt, prompts/prompt_26.txt, and prompts/prompt_27.txt for the staged timing plan.
9) Follow-up to the Phase 21 “Other” answer foundation: allow admins to attach hidden tags to the system-generated Other choice only. Keep Other out of normal answer_options, do not add editable Other labels, and do not make Other trigger conditional logic. When a participant provides Other text, reports and CSV should resolve the configured Other hidden tags while participant-facing APIs continue to hide all tag metadata.
10) Survey completion time stats and Admin override foundation. This can be implemented directly after Phase 24; glossary phases 28-30 are not prerequisites. Build backend-owned survey-level completion time estimates from valid completed attempts using completed_at minus started_at, add a total estimated survey minutes Admin override that wins over derived/default estimates, and show derived/default/effective estimate metadata on Admin Setup. Use backend defaults based on question-type weights when no completed-attempt sample exists.
11) Participant estimated completion display. Replace participant-facing numeric progress labels with estimated time remaining copy powered by the effective survey estimate from the timing foundation. Calculate remaining time from the resolved visible path and remaining question weights, keep branching/page-jump behavior aligned with resolveAttemptPagePath, and leave Admin pagination labels unchanged.
12) Running dynamic time remaining instrumentation. Add lightweight authenticated and anonymous participant activity timing so future estimates can improve beyond coarse attempt start/completion timestamps. Track safe page/session activity, cap idle gaps so long pauses do not count as active time, and keep timing internals out of participant payloads, reporting, hidden tags, and CSV.
13) Global glossary Admin foundation. Build an Admin-only glossary page and backing schema/API where Admins can define a canonical term, match strings/aliases, an approved definition, and enabled/disabled state. Keep glossary entries global, plain-text, and separate from surveys, questions, answers, tags, response data, reporting, and CSV.
14) Dictionary-assisted glossary definitions. Add an Admin-only lookup flow using the approved current Webster/dictionary provider so Admins can request suggested definitions, then accept, edit, or ignore them. Keep provider access server-side, secrets in environment configuration, automated tests mocked, and manual definition entry available when no definition is found or the provider is unavailable.
15) Participant inline glossary rendering. Deliver enabled global glossary definitions to the participant runner through a participant-safe payload and dynamically wrap matching terms in question text with accessible hover/focus/click/tap popovers. Use deterministic plain-text matching, handle aliases/overlaps, preserve survey text, and avoid any changes to validation, skip logic, response storage, reporting, hidden tags, or CSV.
