# Phase 18 — Password Reset Foundation

## Purpose

Build secure password reset foundation from both the login screen and a logged-in
profile/settings context.

This phase depends on the Phase 16 email client foundation.

Source feedback or planning note:

```txt
notes/next_phases.txt item 3
```

## Role Note

Default arrangement:

* Codex is the implementation assistant.
* Claude Code is the review assistant.

Branch:

```bash
git checkout main
git checkout -b phase-18-password-reset
```

Closeout artifacts:

* implementation handoff: `notes/claude_handoff_phase_18.txt`
* Claude review: `notes/claude_review_phase_18.txt`

---

# Required Reading

Read before implementation:

* `notes/next_phases.txt`
* `prompts/MASTER_PRODUCT_CONTEXT.txt`
* `markdown/ARCHITECTURE_PRINCIPLES.md`
* `markdown/DATA_MODEL_VISION.md`
* `markdown/GLOBAL_DEVELOPMENT_ENVIRONMENT.txt`
* `markdown/FLOW.md`
* `markdown/FOLLOW_UPS.md`
* `markdown/PHASE_LOG.md`
* `markdown/REVIEW_CHECKLIST.md`
* `prompts/prompt_16.txt`
* `prompts/prompt_17.txt`
* Current auth routes, password hashing helpers, auth rate limits, and email
  service.
* `apps/api/src/services/anonymousSurveys.ts` for the Phase 17 lookup-key plus
  hashed-secret token pattern.
* `apps/api/src/services/rateLimitStore.ts` if public reset endpoints should use
  the shared PostgreSQL-backed rate-limit store.

---

# Received Feedback

Users should be able to request a password reset from a logged-in state via
profile/settings and from the login screen with "Forgot password?".

---

# Intake Classification

* security-sensitive auth feature
* database schema and API contract change
* email service usage
* frontend auth/profile workflow

Expected impact:

* password reset token storage
* auth endpoints
* login UI
* logged-in profile/settings reset initiation
* tests for enumeration resistance and token lifecycle

---

# Scope

Implement:

* Password reset token table or equivalent relational model storing hashed
  tokens, expiry, consumed timestamp, and user association.
* Public lookup key plus hashed secret token model, similar to Phase 17
  anonymous links.
* Request endpoint from login that always returns a generic success response.
* Complete endpoint that verifies a token, hashes the new password with bcrypt,
  consumes the token, and rejects reuse.
* Logged-in profile/settings reset initiation using the same reset service.
* Phase 16 email service integration for reset email dispatch.
* Reset links generated from `WEB_ORIGIN`, with a frontend reset-password route
  that accepts the reset token from the URL.
* Development-safe reset testing path that does not log reset secrets in
  production.
* Rate limiting consistent with existing auth protection.

## Non-Goals

Do not implement:

* unrelated profile redesign beyond the reset entry point
* admin-triggered password reset; that belongs to Phase 20
* plaintext token storage
* encrypted or recoverable reset-token storage; password reset tokens should
  remain one-time hash-only credentials
* production reset-token logging
* account recovery questions or MFA
* a production email provider adapter; Phase 16 only added the email abstraction
  and disabled/no-op behavior

## Assumptions

* Password reset responses must not reveal whether an email exists.
* Tokens are single-use and expire.
* Resetting a password should not expose password hashes or reset secrets in any
  API response.
* Phase 16 provides the email client abstraction, but real email delivery is
  still disabled/no-op only.
* App-wide `Referrer-Policy: no-referrer` from Phase 17 should protect
  reset-token URLs from referrer leakage.
* Do not reuse `ANONYMOUS_LINK_TOKEN_ENCRYPTION_SECRET`; reset tokens should not
  be stored in recoverable form.
* If no profile/settings page exists, add only the smallest logged-in account or
  settings entry point needed for reset initiation.

## Open Questions

* Should existing login sessions be invalidated after password reset? If no
  token revocation model exists, default to not invalidating existing sessions,
  document the tradeoff, and keep scope minimal.
* Should public reset endpoints use existing auth rate-limit behavior only, or
  the Phase 17 PostgreSQL-backed store for horizontal scaling? Prefer the
  existing auth behavior unless a shared public reset limit is straightforward.

---

# Acceptance Criteria

The phase is ready for review when:

* Login screen has a "Forgot password?" flow.
* Logged-in user can initiate a reset from profile/settings.
* Existing and non-existing emails receive identical status codes and response
  bodies.
* A valid reset token works once and then cannot be reused.
* Expired, consumed, malformed, or unknown tokens fail safely.
* New passwords use existing bcrypt hashing and validation rules.

---

# Testing Requirements

Run:

```bash
npm run typecheck
npm run lint
npm run build
npm test
git diff --check
```

Add focused tests for:

* request flow enumeration resistance
* token hash storage and single-use consumption
* expired/used/invalid token failure
* successful password update and old password rejection
* logged-in reset initiation auth requirement
* rate limiting where practical
* reset email payload sent through the Phase 16 email client abstraction
* reset-token URL generated from `WEB_ORIGIN`
* reset token not stored plaintext, encrypted, or otherwise recoverable
* reset request and completion endpoints do not set unexpected cookies

Manual checks:

* Request reset for existing and non-existing email; both show generic success.
* Use reset link once; confirm new password works and old password fails.
* Reuse expired/consumed token; confirm safe error.
* Trigger reset while logged in from profile/settings.
* Confirm no plaintext reset token is stored in the database.
* Confirm no reset token is stored in encrypted/recoverable form.
* Confirm the reset link route works without leaking the token into logs or
  referrers.

---

# Claude Review Handoff

Before implementation closeout, create:

```txt
notes/claude_handoff_phase_18.txt
```

Ask Claude Code to review:

* token hashing
* enumeration resistance
* rate limiting
* cookie/session behavior after reset
* email safety
* use of Phase 17 token/referrer/rate-limit lessons where appropriate
* accidental implementation of Phase 20 admin reset scope

Claude Code must write review output to:

```txt
notes/claude_review_phase_18.txt
```

---

# Documentation And Closeout

Update as needed:

* `.env.example` or environment docs if reset URLs add config
* `markdown/FOLLOW_UPS.md`
* `markdown/PHASE_LOG.md` with validation, manual tests, handoff path, Claude
  review status, and commit readiness
